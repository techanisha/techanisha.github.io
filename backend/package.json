{
  "name": "niva-api",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute niva-orders --local --file ./schema.sql",
    "db:remote": "wrangler d1 execute niva-orders --remote --file ./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
